// Shared Gradle configuration for implementation MimicAPI

ext.api = project(":api")
apply from: rootProject.file("gradle/mimic.gradle")

// Finish configuring MimicAPI first
evaluationDependsOn api.path

// Use api as dependency
dependencies {
    compile api
}
